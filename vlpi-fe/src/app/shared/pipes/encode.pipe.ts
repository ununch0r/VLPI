import { Pipe, PipeTransform } from '@angular/core';

@Pipe({name: 'encode'})
export class EncodePipe implements PipeTransform {
  transform(value: string, shouldEncode: boolean): string {
      if(shouldEncode)
      {
          return '???????? ????? ???????? ????? ???????? ?????'
      }else{
          return value;
      }
  }
}